<!-- Generated by sdl-zig-codegen. Do not edit. -->

# SDL binding coverage

This report is generated from the configured SDL headers, target matrix, analyzed public declarations, and symbols emitted by the binding generator.

The inventory covers the generator's semantic Zig surface: public declarations and configured public macros recognized by the analysis pass. The raw C import remains available through each module's `c` declaration and is not counted as a generated Zig binding.

An intentional exclusion does not mean that no approximation is possible. It means that the generator does not claim a faithful standalone Zig binding for the C entry: an approximation may lose compile-time elimination, caller source information, target-specific behavior, static-analysis meaning, ownership, or linker effects. Such entries may still be available through the module's raw `c` import when the C translation exposes them, or may be covered by a higher-level Zig API without counting the macro itself as covered.

Coverage is reported two ways: **surface coverage** includes every analyzed API entry, while **supported-scope coverage** removes entries explicitly excluded by generator policy from the denominator. Every entry not emitted is listed below as either intentional or a generator limitation.

## Summary

| Library | API entries | Covered | Intentional | Limitations | Surface coverage | Supported-scope coverage |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ControllerImage | 28 | 28 | 0 | 0 | 100.00% | 100.00% |
| SDL_image | 181 | 181 | 0 | 0 | 100.00% | 100.00% |
| SDL_mixer | 187 | 187 | 0 | 0 | 100.00% | 100.00% |
| SDL_net | 64 | 64 | 0 | 0 | 100.00% | 100.00% |
| SDL | 5363 | 5297 | 65 | 1 | 98.77% | 99.98% |
| SDL_shadercross | 66 | 66 | 0 | 0 | 100.00% | 100.00% |
| SDL_test | 137 | 136 | 0 | 1 | 99.27% | 99.27% |
| SDL_ttf | 257 | 257 | 0 | 0 | 100.00% | 100.00% |
| Overall | 6283 | 6216 | 65 | 2 | 98.93% | 99.97% |

## Scope

- **ControllerImage** (`controller_image`): controllerimage.h; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL_image** (`image`): SDL3_image/SDL_image.h; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL_mixer** (`mixer`): SDL3_mixer/SDL_mixer.h; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL_net** (`net`): SDL3_net/SDL_net.h; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL** (`sdl`): SDL3 public headers; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL_shadercross** (`shadercross`): SDL3_shadercross/SDL_shadercross.h; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL_test** (`test`): SDL3/SDL_test.h; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.
- **SDL_ttf** (`ttf`): SDL3_ttf public headers; targets: `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu`.

## Intentional exclusions

These entries are omitted by explicit profile policy because they are compiler annotations, preprocessor/build-time machinery, debugger behavior, or another non-portable C concern rather than consumer-facing runtime bindings.

| Library | C API entry | Kind | Status | Handling | Targets | Evidence | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SDL | `SDL_ACQUIRE` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ACQUIRE — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ACQUIRE_SHARED` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ACQUIRE_SHARED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ACQUIRED_AFTER` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ACQUIRED_AFTER — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ACQUIRED_BEFORE` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ACQUIRED_BEFORE — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ALIGNED` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ALIGNED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ALLOC_SIZE` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ALLOC_SIZE — Application inventory: 1 normalized declaration application(s); consumption: normalized allocation-size validation and allocator return planning. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ALLOC_SIZE2` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ALLOC_SIZE2 — Application inventory: 1 normalized declaration application(s); consumption: normalized allocation-size validation and allocator return planning. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ANALYZER_NORETURN` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_ANALYZER_NORETURN — Application inventory: 1 normalized declaration application(s); consumption: normalized return-flow documentation and planning. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_assert` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_assert — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. | Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_assert_always` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_assert_always — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. | Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_ASSERT_CAPABILITY` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ASSERT_CAPABILITY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_assert_paranoid` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_assert_paranoid — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. | Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_assert_release` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_assert_release — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. | Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_ASSERT_SHARED_CAPABILITY` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_ASSERT_SHARED_CAPABILITY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_BeginThreadFunction` | object_macro | intentional | indirect | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | wrapper: SDL_BeginThreadFunction — Application inventory: 0 declaration application(s); consumption: generated thread-creation wrapper forwarding; no standalone hook name is emitted. | No faithful standalone port: the C headers select compiler, runtime, or thread-entry annotations. A Zig declaration-level policy could approximate some of them, but these macros have no consumer-facing runtime value. |
| SDL | `SDL_CAPABILITY` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_CAPABILITY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_DECLSPEC` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_DECLSPEC — Application inventory: 1261 normalized declaration application(s); consumption: normalized linkage selection. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_DEPRECATED` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_DEPRECATED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_disabled_assert` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_disabled_assert — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. | Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_DLNOTE_JOIN` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JOIN — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JOIN2` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JOIN2 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY_GET` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY_GET — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY1` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY1 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY2` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY2 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY3` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY3 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY4` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY4 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY5` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY5 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY6` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY6 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY7` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY7 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY8` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_DLNOTE_JSON_ARRAY8 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_ELF_NOTE_DLOPEN` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_ELF_NOTE_DLOPEN — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_ELF_NOTE_INTERNAL` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_ELF_NOTE_INTERNAL — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_ELF_NOTE_INTERNAL2` | function_macro | intentional | unrepresentable | `x86_64-linux-gnu` | policy: SDL_ELF_NOTE_INTERNAL2 — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. | No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_enabled_assert` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_enabled_assert — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. | Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_EndThreadFunction` | object_macro | intentional | indirect | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | wrapper: SDL_EndThreadFunction — Application inventory: 0 declaration application(s); consumption: generated thread-creation wrapper forwarding; no standalone hook name is emitted. | No faithful standalone port: the C headers select compiler, runtime, or thread-entry annotations. A Zig declaration-level policy could approximate some of them, but these macros have no consumer-facing runtime value. |
| SDL | `SDL_EXCLUDES` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_EXCLUDES — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_FALLTHROUGH` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_FALLTHROUGH — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_FORCE_INLINE` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_FORCE_INLINE — Application inventory: 13 normalized declaration application(s); consumption: normalized declaration metadata and validation. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_GUARDED_BY` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_GUARDED_BY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_HAS_BUILTIN` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_HAS_BUILTIN — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_INLINE` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_INLINE — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_MALLOC` | object_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_MALLOC — Application inventory: 5 normalized declaration application(s); consumption: normalized allocator/alias metadata validation. | No faithful standalone port: the C headers select compiler, runtime, or thread-entry annotations. A Zig declaration-level policy could approximate some of them, but these macros have no consumer-facing runtime value. |
| SDL | `SDL_NO_THREAD_SAFETY_ANALYSIS` | object_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_NO_THREAD_SAFETY_ANALYSIS — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: the C headers select compiler, runtime, or thread-entry annotations. A Zig declaration-level policy could approximate some of them, but these macros have no consumer-facing runtime value. | No faithful standalone port: the C headers select compiler, runtime, or thread-entry annotations. A Zig declaration-level policy could approximate some of them, but these macros have no consumer-facing runtime value. |
| SDL | `SDL_NODISCARD` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_NODISCARD — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_NORETURN` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_NORETURN — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_PRINTF_VARARG_FUNC` | function_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_PRINTF_VARARG_FUNC — Application inventory: 20 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_PRINTF_VARARG_FUNCV` | function_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_PRINTF_VARARG_FUNCV — Application inventory: 5 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_PT_GUARDED_BY` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_PT_GUARDED_BY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RELEASE` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_RELEASE — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RELEASE_GENERIC` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_RELEASE_GENERIC — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RELEASE_SHARED` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_RELEASE_SHARED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_REQUIRES` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_REQUIRES — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_REQUIRES_SHARED` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_REQUIRES_SHARED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RESTRICT` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_RESTRICT — Application inventory: 5 normalized declaration application(s); consumption: normalized allocator/alias metadata validation. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_RETURN_CAPABILITY` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_RETURN_CAPABILITY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_SCANF_VARARG_FUNC` | function_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_SCANF_VARARG_FUNC — Application inventory: 1 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_SCANF_VARARG_FUNCV` | function_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_SCANF_VARARG_FUNCV — Application inventory: 1 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_SCOPED_CAPABILITY` | object_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_SCOPED_CAPABILITY — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_stack_alloc` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_stack_alloc — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Approximation is possible with an allocator, but not faithfully: the C pair selects stack or SDL-heap storage by target/compiler and relies on matching lifetime and release semantics. An allocator-backed Zig helper would change those contracts. | Approximation is possible with an allocator, but not faithfully: the C pair selects stack or SDL-heap storage by target/compiler and relies on matching lifetime and release semantics. An allocator-backed Zig helper would change those contracts. |
| SDL | `SDL_stack_free` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_stack_free — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Approximation is possible with an allocator, but not faithfully: the C pair selects stack or SDL-heap storage by target/compiler and relies on matching lifetime and release semantics. An allocator-backed Zig helper would change those contracts. | Approximation is possible with an allocator, but not faithfully: the C pair selects stack or SDL-heap storage by target/compiler and relies on matching lifetime and release semantics. An allocator-backed Zig helper would change those contracts. |
| SDL | `SDL_STRINGIFY_ARG` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_STRINGIFY_ARG — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: C stringifies the caller's preprocessor token before evaluation. A Zig function receives a value or an explicitly supplied string and cannot recover the original token spelling. | No faithful standalone port: C stringifies the caller's preprocessor token before evaluation. A Zig function receives a value or an explicitly supplied string and cannot recover the original token spelling. |
| SDL | `SDL_TRY_ACQUIRE` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_TRY_ACQUIRE — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_TRY_ACQUIRE_SHARED` | function_macro | intentional | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_TRY_ACQUIRE_SHARED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. | No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_UNUSED` | object_macro | intentional | semantic | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | normalized_effect: SDL_UNUSED — Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. | No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |

## Policy reconciliation

Every configured intentional policy has one generator-owned relation. A relation with one or more normalized declaration applications is marked **consumed**; a zero-application compiler/preprocessor contract is marked **rejected** with its explicit reason.

| Library | Policy | Applications | Disposition | Detail |
| --- | --- | ---: | --- | --- |
| SDL | `SDL_ACQUIRE` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ACQUIRE_SHARED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ACQUIRED_AFTER` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ACQUIRED_BEFORE` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ALIGNED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_ALLOC_SIZE` | 1 | consumed | Application inventory: 1 normalized declaration application(s); consumption: normalized allocation-size validation and allocator return planning. |
| SDL | `SDL_ALLOC_SIZE2` | 1 | consumed | Application inventory: 1 normalized declaration application(s); consumption: normalized allocation-size validation and allocator return planning. |
| SDL | `SDL_ANALYZER_NORETURN` | 1 | consumed | Application inventory: 1 normalized declaration application(s); consumption: normalized return-flow documentation and planning. |
| SDL | `SDL_assert` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_assert_always` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_ASSERT_CAPABILITY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_assert_paranoid` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_assert_release` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_ASSERT_SHARED_CAPABILITY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_BeginThreadFunction` | 0 | consumed | Application inventory: 0 declaration application(s); consumption: generated thread-creation wrapper forwarding; no standalone hook name is emitted. |
| SDL | `SDL_CAPABILITY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_DECLSPEC` | 1261 | consumed | Application inventory: 1261 normalized declaration application(s); consumption: normalized linkage selection. |
| SDL | `SDL_DEPRECATED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_disabled_assert` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_DLNOTE_JOIN` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JOIN2` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY_GET` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY1` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY2` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY3` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY4` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY5` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY6` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY7` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_DLNOTE_JSON_ARRAY8` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_ELF_NOTE_DLOPEN` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_ELF_NOTE_INTERNAL` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_ELF_NOTE_INTERNAL2` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No consumer-facing runtime port: these helpers paste tokens, dispatch variadic arguments, and emit ELF note/linker metadata. A Zig function could build a string, but it would not recreate the C object, symbol, or linker-note behavior. |
| SDL | `SDL_enabled_assert` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Rejected after a pinned Zig 0.16.0 proof attempt: no generic inline helper can provide C's per-call-site static SDL_AssertData, #condition token stringification, and caller function/file/line simultaneously. SDL_ReportAssertion mutates and retains that data in a process-wide linked report, so a temporary stack record aliases after return and a shared record loses site independence and is not thread-safe. Macro-level SDL_ASSERT_LEVEL elision must also avoid evaluating disabled conditions, while break/abort remain target-specific control flow. Keep the raw assert runtime APIs and use Zig assertions for ordinary invariants; no honest additive adapter is emitted. |
| SDL | `SDL_EndThreadFunction` | 0 | consumed | Application inventory: 0 declaration application(s); consumption: generated thread-creation wrapper forwarding; no standalone hook name is emitted. |
| SDL | `SDL_EXCLUDES` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_FALLTHROUGH` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_FORCE_INLINE` | 13 | consumed | Application inventory: 13 normalized declaration application(s); consumption: normalized declaration metadata and validation. |
| SDL | `SDL_GUARDED_BY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_HAS_BUILTIN` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_INLINE` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_MALLOC` | 5 | consumed | Application inventory: 5 normalized declaration application(s); consumption: normalized allocator/alias metadata validation. |
| SDL | `SDL_NO_THREAD_SAFETY_ANALYSIS` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: the C headers select compiler, runtime, or thread-entry annotations. A Zig declaration-level policy could approximate some of them, but these macros have no consumer-facing runtime value. |
| SDL | `SDL_NODISCARD` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_NORETURN` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_PRINTF_VARARG_FUNC` | 14 | consumed | Application inventory: 14 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. |
| SDL | `SDL_PRINTF_VARARG_FUNCV` | 5 | consumed | Application inventory: 5 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. |
| SDL | `SDL_PT_GUARDED_BY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RELEASE` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RELEASE_GENERIC` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RELEASE_SHARED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_REQUIRES` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_REQUIRES_SHARED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_RESTRICT` | 5 | consumed | Application inventory: 5 normalized declaration application(s); consumption: normalized allocator/alias metadata validation. |
| SDL | `SDL_RETURN_CAPABILITY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_SCANF_VARARG_FUNC` | 1 | consumed | Application inventory: 1 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. |
| SDL | `SDL_SCANF_VARARG_FUNCV` | 1 | consumed | Application inventory: 1 typed declaration application(s); consumption: FormatContract-driven wrapper planning and validation. |
| SDL | `SDL_SCOPED_CAPABILITY` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |
| SDL | `SDL_stack_alloc` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Approximation is possible with an allocator, but not faithfully: the C pair selects stack or SDL-heap storage by target/compiler and relies on matching lifetime and release semantics. An allocator-backed Zig helper would change those contracts. |
| SDL | `SDL_stack_free` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — Approximation is possible with an allocator, but not faithfully: the C pair selects stack or SDL-heap storage by target/compiler and relies on matching lifetime and release semantics. An allocator-backed Zig helper would change those contracts. |
| SDL | `SDL_STRINGIFY_ARG` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: C stringifies the caller's preprocessor token before evaluation. A Zig function receives a value or an explicitly supplied string and cannot recover the original token spelling. |
| SDL | `SDL_TRY_ACQUIRE` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_TRY_ACQUIRE_SHARED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No faithful standalone port: these are C compiler/static-analysis annotations for lock ordering, capabilities, aliasing, builtins, or varargs checking. A generator could preserve selected metadata, but a public Zig function would not provide the same analysis contract. |
| SDL | `SDL_UNUSED` | 0 | rejected | Application inventory: 0 normalized declaration applications; consumption: explicitly rejected — No standalone runtime port: these are declaration annotations or compiler spellings for visibility, deprecation, fallthrough, inlining, result use, return behavior, aliasing, capabilities, or unused values. Some could influence generator metadata, but exposing them as public functions would be misleading. |

## Generator limitations

This is the exhaustive difference between the analyzed API inventory and emitted symbols after intentional exclusions. Each row is an analyzed public entry that the current generator does not handle.

| Library | C API entry | Kind | Status | Handling | Targets | Evidence | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SDL | `SDL_swprintf` | function | limitation | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDL_swprintf — Application inventory: not observed; consumption: not applicable; No honest consumer-facing or generator operation exists for this entry. | The function planner or renderer has no supported public binding for this signature. |
| SDL_test | `SDLTest_LogMessage` | function | limitation | unrepresentable | `aarch64-ios`, `aarch64-ios-simulator`, `aarch64-linux-android21`, `aarch64-macos`, `aarch64-tvos`, `aarch64-tvos-simulator`, `wasm32-emscripten`, `x86_64-ios-simulator`, `x86_64-linux-gnu`, `x86_64-tvos-simulator`, `x86_64-windows-gnu` | policy: SDLTest_LogMessage — Application inventory: not observed; consumption: not applicable; No honest consumer-facing or generator operation exists for this entry. | The function planner or renderer has no supported public binding for this signature. |
